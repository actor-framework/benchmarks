//module org.caf.salsa;

behavior chain_link {
  ActorReference m_next;

  chain_link(ActorReference next) {
    m_next = next;
  }

  ack _token(int v) {
    m_next <- _token(v);
    if (v == 0) {
      //self <- destroy(); // COMPILER ERROR: Could not find matching message, message name: 'destroy'
    }
  }
}
